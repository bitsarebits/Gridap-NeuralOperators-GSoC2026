rules_version='2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Public read-only access. 
      // Write access is exclusively granted via the Julia backend using the Admin SDK / Service Account.
      allow read: if true;
      allow write: if false;
    }
  }
}